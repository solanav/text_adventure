#include <stdio.h>
#include <string.h>
#include "game.h"
#include "dialogue.h"

char * dialogue_generate(Game * game)
{
	char * dialogue;
	int flag = 0;

	if(!game) return NULL;

	switch (CMD) {
		case :
	}
}
